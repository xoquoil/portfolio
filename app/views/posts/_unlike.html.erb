<%= link_to like_path(current_user.likes.find_by(post_id: post.id)), class:"m-0 text-decoration-none", method: :delete, remote: true, id: "js-unlike-button-for-post-#{post.id}"  do %>
  <i class="bi bi-star-fill"></i>
  <span class="text-secondary"><%= post.likes.count %></span>
<% end %>